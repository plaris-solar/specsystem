module.exports = {
    production: { // these settings are used when you run 'npm run build'
        outputDir: '../frontend/',
    },
    development: { // these settings are used when you run 'npm run serve'
        outputDir: '../frontend/',
    }
 }